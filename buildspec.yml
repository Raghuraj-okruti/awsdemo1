version: 0.2
phases:
  build:
    commands:
      - echo Entered the build phase
      - mvn clean install
  post_build:
    commands:
      - echo Build Completed Successfully.
artifacts:
  files:
    - target/awsdemo1.jar
discarded-pahs: yes
